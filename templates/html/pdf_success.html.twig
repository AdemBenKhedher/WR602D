{% extends 'base.html.twig' %}

{% block title %}PDF Généré{% endblock %}

{% block body %}
    <div class="pdf-success-container">
        <h1>PDF Généré avec Succès !</h1>
        <p>Votre PDF a été généré et envoyé à votre adresse e-mail.</p>
        <p>Vous pouvez également le visualiser en cliquant sur le bouton ci-dessous :</p>
        <a href="{{ asset('Pdfs/' ~ filename) }}" target="_blank" class="btn-view-pdf">Ouvrir le PDF</a>
    </div>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .pdf-success-container {
            max-width: 600px;
            margin: 50px auto;
            text-align: center;
            background: #fff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.1);
        }

        .pdf-success-container h1 {
            font-size: 24px;
            color: #333;
            margin-bottom: 20px;
        }

        .pdf-success-container p {
            font-size: 16px;
            color: #555;
            margin-bottom: 30px;
        }

        .btn-view-pdf {
            display: inline-block;
            background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
            color: white;
            padding: 12px 20px;
            border-radius: 5px;
            text-decoration: none;
            font-size: 16px;
            font-weight: bold;
            transition: background-color 0.3s;
        }

        .btn-view-pdf:hover {
            background-color: #3a5ccc;
        }
    </style>
{% endblock %}
